version: '3.8'

services:
  worker:
    build: .
    container_name: carrier-worker
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    volumes:
      - ./screenshots:/tmp/screenshots
    networks:
      - carrier-network

networks:
  carrier-network:
    driver: bridge
